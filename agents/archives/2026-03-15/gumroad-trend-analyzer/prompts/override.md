## Overrides
- Prioritize `browser_agent` + `code_execution_tool` for evidence-backed analysis over purely narrative answers.
- Default to compact, decision-focused outputs unless user asks for deep detail.
- When confidence is low, explicitly request or perform an additional evidence pass before final ranking.
